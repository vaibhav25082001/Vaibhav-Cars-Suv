import useAuth from "../hooks/useAuth";import useApi from "../hooks/useApi";import Card from "../components/ui/Card";export default function Account(){const {user}=useAuth();const {data}=useApi(user?.id?`/customers/${user.id}`:null,{auto:Boolean(user?.id)});const c=data?.data||user||{};return <div className="grid gap-4 lg:grid-cols-2"><Card title="Profile"><p>{c.name}</p><p className="text-white/60">{c.email}</p><p>{c.phone}</p><p className="text-vaibhav-gold">{c.loyaltyPoints||0} loyalty points</p></Card><Card title="Vehicles">{(c.vehicles||[]).map(v=><p key={v.id}>{v.registrationNumber} · {v.color}</p>)}</Card></div>}