import { useParams, Link } from "react-router-dom";import useApi from "../hooks/useApi";import Card from "../components/ui/Card";import Button from "../components/ui/Button";export default function ModelDetails(){const {id}=useParams();const {data:car}=useApi(`/cars/${id}`);const c=car?.data||car||{};return <div className="space-y-5"><div className="aspect-[16/7] rounded-xl border border-vaibhav-gold/30 bg-black/70 p-6"><img src={c.imageUrls?.[0]} className="h-full w-full object-contain" onError={e=>e.currentTarget.style.display="none"}/></div><Card title={c.name}><div className="grid gap-3 md:grid-cols-4"><p>Type: {c.type}</p><p>Fuel: {c.fuelType}</p><p>BHP: {c.bhp}</p><p>Price: ₹{Number(c.price||0).toLocaleString("en-IN")}</p></div><div className="mt-4 flex gap-3"><Link to="/configurator"><Button>Configure</Button></Link><Link to="/test-drive"><Button variant="ghost">Book Test Drive</Button></Link></div></Card></div>}