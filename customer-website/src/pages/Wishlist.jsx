import useApi from "../hooks/useApi";import EmptyState from "../components/ui/EmptyState";import Card from "../components/ui/Card";export default function Wishlist(){const user=JSON.parse(localStorage.getItem("vaibhav_user")||"{}");const {data}=useApi(user.id?`/cars/wishlists?customerId=${user.id}`:null,{auto:Boolean(user.id)});const rows=data?.data||data||[];return <Card title="Wishlist">{rows.length?rows.map(w=><p key={w.id}>{w.carModel?.name}</p>):<EmptyState/>}</Card>}