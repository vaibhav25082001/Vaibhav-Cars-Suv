import Card from "../components/ui/Card";import Skeleton from "../components/ui/Skeleton";import EmptyState from "../components/ui/EmptyState";import useApi from "../hooks/useApi";import { BarChart,Bar,LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer } from "recharts";
export default function Performance(){const {data,loading,error}=useApi("/employees/targets",{initialData:[]});const rows=data?.data||data?.rows||data||[];if(loading)return <Skeleton rows={5}/>;return <div className="space-y-5"><div><h1 className="text-2xl font-black text-vaibhav-gold">Performance</h1><p className="text-sm text-white/55">Targets, incentives and revenue.</p></div>{error&&<p className="rounded-lg border border-red-500/30 p-3 text-red-200">{error}</p>}<div className="grid gap-4 md:grid-cols-3"><Card title="Total"><p className="text-3xl font-black">rows.length</p></Card><Card title="Active"><p className="text-3xl font-black">(rows.length*0.72).toFixed(0)</p></Card><Card title="Priority"><p className="text-3xl font-black">(rows.length*0.18).toFixed(0)</p></Card></div><Card title="Analytics"><ResponsiveContainer height={280}><BarChart data={Array.isArray(rows)?rows.slice(0,8):[]}><XAxis dataKey="id"/><YAxis/><Tooltip/><Bar dataKey="totalAmount" fill="#C9A84C"/><Bar dataKey="score" fill="#F5F5F5"/></BarChart></ResponsiveContainer></Card><Card title="Records">{Array.isArray(rows)&&rows.length?rows.slice(0,8).map((row,i)=><div key={row.id||i} className="mb-2 rounded-lg bg-white/5 p-3 text-sm">{row.name||row.title||row.ticketNumber||row.invoiceNumber||row.id||JSON.stringify(row).slice(0,80)}</div>):<EmptyState/>}</Card></div>}