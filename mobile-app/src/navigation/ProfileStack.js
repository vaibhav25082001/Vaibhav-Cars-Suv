import { createNativeStackNavigator } from "@react-navigation/native-stack";import Profile from "../screens/Profile";import Loyalty from "../screens/Loyalty";import Referrals from "../screens/Referrals";const Stack=createNativeStackNavigator();export default function ProfileStack(){return <Stack.Navigator screenOptions={{headerShown:false}}><Stack.Screen name="Profile" component={Profile}/><Stack.Screen name="Loyalty" component={Loyalty}/><Stack.Screen name="Referrals" component={Referrals}/></Stack.Navigator>}