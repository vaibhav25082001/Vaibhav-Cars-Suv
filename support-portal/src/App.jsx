import { Navigate, Route, Routes } from "react-router-dom";import AppLayout from "./components/layout/AppLayout";import ErrorBoundary from "./components/ui/ErrorBoundary";import Login from "./pages/Login";import AgentDashboard from "./pages/AgentDashboard";import TicketManagement from "./pages/TicketManagement";import TicketDetail from "./pages/TicketDetail";import LiveChat from "./pages/LiveChat";import Customer360 from "./pages/Customer360";import Analytics from "./pages/Analytics";import KnowledgeBase from "./pages/KnowledgeBase";import EscalationManager from "./pages/EscalationManager";import Settings from "./pages/Settings";
function Guard({children}){const user=JSON.parse(localStorage.getItem("vaibhav_user")||"null");return user?children:<Navigate to="/login"/>}
export default function App(){return <ErrorBoundary><Routes><Route path="/login" element={<Login/>}/><Route element={<Guard><AppLayout/></Guard>}><Route index element={<AgentDashboard/>}/><Route path="ticketmanagement" element={<TicketManagement/>}/><Route path="ticketdetail" element={<TicketDetail/>}/><Route path="livechat" element={<LiveChat/>}/><Route path="customer360" element={<Customer360/>}/><Route path="analytics" element={<Analytics/>}/><Route path="knowledgebase" element={<KnowledgeBase/>}/><Route path="escalationmanager" element={<EscalationManager/>}/><Route path="settings" element={<Settings/>}/></Route></Routes></ErrorBoundary>}