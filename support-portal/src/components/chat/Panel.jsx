import Card from "../ui/Card";export default function Panel({title="chat",children}){return <Card title={title}>{children||<p className="text-sm text-white/60">Live VAIBHAV chat module connected to backend APIs.</p>}</Card>}