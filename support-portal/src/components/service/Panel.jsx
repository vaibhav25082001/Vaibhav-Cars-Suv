import Card from "../ui/Card";export default function Panel({title="service",children}){return <Card title={title}>{children||<p className="text-sm text-white/60">Live VAIBHAV service module connected to backend APIs.</p>}</Card>}